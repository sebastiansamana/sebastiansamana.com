<div class="math-visualisation-background" aria-hidden="true" data-varelism-math-background>
  <canvas
    class="math-visualisation-background__canvas"
    width="992"
    height="1772"
    data-math-hero-canvas
    data-math-desktop-point-scale="0.72"
    data-math-initial-offset-ms="146000"
  ></canvas>
</div>

<script src="/scripts/math-visualisation-canvas.js?v=20260710-varelism-red-smooth-point68-lab-scale-offset146" defer data-astro-rerun></script>

<style>
  .math-visualisation-background {
    background: #ffffff;
    contain: layout style;
    height: 100%;
    inset: 0;
    min-height: 100vh;
    min-height: 100svh;
    min-height: 100dvh;
    overflow: visible;
    pointer-events: none;
    position: absolute;
    transform: translateZ(0);
    width: 100%;
    z-index: 0;
  }

  .math-visualisation-background__canvas {
    display: block;
    height: auto;
    left: 50%;
    max-height: none;
    max-width: none;
    opacity: 1;
    position: absolute;
    top: 50%;
    transform-origin: center center;
    transform: translate3d(-50%, -50%, 0);
    width: max(102vw, calc(102dvh * 992 / 1772));
    will-change: transform;
  }

  :global(.site-shell) {
    isolation: isolate;
  }

  :global(.site-shell > main) {
    position: relative;
  }

  @media (max-width: 560px) {
    .math-visualisation-background__canvas {
      width: max(116vw, calc(104dvh * 992 / 1772));
    }
  }

  @media (max-width: 640px) and (orientation: portrait) {
    .math-visualisation-background__canvas {
      transform: translate3d(-50%, -50%, 0) rotate(90deg);
      width: max(104dvh, calc(118vw * 992 / 1772));
    }
  }
</style>
